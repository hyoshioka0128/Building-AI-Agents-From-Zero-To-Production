# =============================================================================
# Building AI Agents From Zero To Production — Python dependencies
# =============================================================================
# Pinned for reproducibility. Unpinned prereleases ("--pre") caused the course
# to drift onto an incompatible SDK surface for new learners. See
# MIGRATION-GUIDE.md for the Microsoft Foundry API mapping used by the samples.
#
# Microsoft Agent Framework — the "foundry" extra brings the Microsoft Foundry
# client surface (FoundryChatClient, FoundryAgent, Toolbox helpers,
# FoundryMemoryProvider, FoundryEvals). "devui" provides the local dev server.
agent-framework==1.2.0
agent-framework-foundry==1.2.0
agent-framework-devui

# Azure identity for AzureCliCredential / DefaultAzureCredential
azure-identity

# Azure AI Projects + Agents SDK (evaluations, vector-store setup, hosted deploy)
azure-ai-projects
azure-ai-agents

# Environment variable management
python-dotenv
